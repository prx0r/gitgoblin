# RUN 2026-09-10 — UK LSE order-level data (GSK, 726,666 rows, score ~33)

Mission: publicly accessible UK equity microstructure (LSE MBO/L2 preferred).
Result: `raphi6/SQL-Database-for-HF-LimitOrderBook-data-from-LSE` (0 stars)
commits `Reconstructing/Data.zip`: GlaxoSmithKline OrderDetail 274,322 rows +
OrderHistory 322,208 + TradeReport 130,136 (~60 dates Jan–Mar 2007, ISIN
GB0009252882, GBX prices, order IDs, LO types, lifecycle + trade tables).
Provenance (sha256 per file) held outside git; data stays local (license unclear).

## Exact log (reproducible, $0, read-only; token via env, never pasted)
1. **Repo search (not code search)** for the exact phrase:
   `GET /search/repositories?q="LSE limit order book"` → 1 hit (this repo).
   Code search for the same terms returned only awesome-lists (noise). Lesson:
   niche student repos are found by exact-phrase REPO search; code search finds
   pipelines that reference data, not the data itself.
2. **Profile check**: 0 stars, student reconstruction project (screenshots, pgAdmin
   PNGs, `lse-data-description.pdf`, notebook). Committed zip, no LFS. All green flags.
3. **Sparse clone, list, size**: `Data.zip` 16MB → 71MB CSVs. Headers inspected
   (`cut -c1-400`): ISIN, GBX prices, order IDs, DDMMYYYY dates, HH:MM:SS times.
4. **Verify**: `wc -l` (274322/322208/130136), distinct date fields (60/22/24),
   sha256 per file → `provenance.json` (source URL + commit + row counts + hashes).
5. **Score** with DATA_GOBLIN_SCORE: +10 data +10 public +8 UK/LSE +5 order-id/
   lifecycle − single security, 2007 only, second-resolution, unclear license.

## Why it worked
- Searched for the TRAIL (exact phrase, student project shape), not the dataset.
- Dead-end discipline: CS289/README-only, eddieeeezz/code-only, LOBSTER-noise all
  recorded as verdicts instead of re-chased (see full hunt log with operator).
- Small committed zips beat LFS/releases hunting when the author is a student.

## How to continue (UK L2 prize still open: multi-stock MBO)
- Query families: exact filenames (`test_Xy.parquet`, `MBO.csv`, `VOD.L orderbook`),
  storage scars (wget/s3/drive/figshare/zenodo/HF), LSE fingerprints
  (LLOY/BARC/TSCO/VOD/BT + DeepLOB), researcher projects (thesis/MSc/capstone),
  parser-adjacent fixtures (sample/, tests/fixtures/, notebooks/output/).
- Rank stale/low-star repos WITH data/ or notebooks/ higher; inspect git history,
  branches, tags, releases, LFS pointers, download scripts, old branches.
- Verify every hit: hash, headers, row counts, date span, license triage
  (unclear → local-only, never push data). Return top-20 scored, not 500 READMEs.

## Reuse decision rule (from round 2)
- Target is LIVE builders/papers → GitGoblin builder-graph (seed + scan + rank).
- Target is ARCHAEOLOGY (dead repos, committed files) → direct API fingerprints
  (this run's pattern). Builder-graph returned 0 signals here by design.
